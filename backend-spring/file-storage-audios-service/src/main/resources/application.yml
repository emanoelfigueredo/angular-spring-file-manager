server:
  port: 8082

spring:
  main:
    allow-bean-definition-overriding: true
  output:
    ansi:
      enabled: always
  application:
    name: FILE-STORAGE-SERVICE-AUDIOS
  data:
    mongodb:
      database: FileStorageAudios
      host: localhost
      port: 27017

logging:
  level:
    io:
      reflectoring: DEBUG
    org:
      springframework:
        web: INFO
    reactor:
      netty:
        http:
          client: DEBUG
